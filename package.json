{
  "name": "mr-tapioca",
  "version": "0.1.0",
  "private": true,
  "description": "Mr. Tapioca — boba focus app. Web app + Capacitor iOS shell (with a native Screen Time blocker).",
  "scripts": {
    "copyweb": "rm -rf www && mkdir www && cp index.html styles.css app.js config.js squad-cloud.js sw.js manifest.json www/ && cp -R assets www/assets",
    "ios:register": "node tools/register-ios-plugins.mjs",
    "ios:sync": "npm run copyweb && npx cap sync ios ; node tools/register-ios-plugins.mjs",
    "ios:open": "npm run ios:sync ; npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
